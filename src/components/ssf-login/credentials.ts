export class Credentials {
  sessionId: string;
  created: Date;
  so: string;
  deviceIdentifier: string;
  pushToken: string;
  deviceName: string;
  baseUrl: string;
  ssn: string;
  personName: string;
}
